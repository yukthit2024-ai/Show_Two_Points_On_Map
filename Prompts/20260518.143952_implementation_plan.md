# User Prompt
```
*	Save any MD file created by AI in the project folder itself.
*	For example 'Implementation Plan.md' or any such file
*	in the MD file, also capture the prompt given by the user
*	Create a folder in the project folder 'Prompts' and create the MD files inside it.  
*	the filename should be prefixed with *_yyyyMMdd.hhmmss
*	Enable this settings via some file based flag(if required) so that in future I do not have to specify this again.
*	Do this for all project I will be working on
```

# Implementation Plan - Configuring Global and Local AI Markdown File Rules

This plan outlines the steps to guarantee that all future AI-generated Markdown files (e.g., implementation plans, documentation, artifacts) are automatically saved under the `Prompts` folder of the current project directory with the requested `yyyyMMdd.hhmmss` prefix and the user's prompt captured at the top.

## Proposed Strategy

To ensure this configuration applies to **all projects** and acts as a **file-based flag** that persists across all future AI conversations, we will implement the following:

1. **Global Knowledge Item (KI) Update**:
   Update the global Knowledge Item `ki_md_rule` at `C:\Users\ShibuPC\.gemini\antigravity\knowledge\ki_md_rule\artifacts\md_file_rules.md` to perfectly specify these exact requirements. KIs are loaded dynamically at the start of every session as "Persistent Context" for the AI, acting as a global user instruction.

2. **Project-level Configuration Flags**:
   Create a standard set of project-level files (`.clinerules` and `.cursorrules`) in the root of the project folder `x:\Projects_X\0_Active\8_Android_APK\Show_Two_Points_On_Map_APK_GITHUB`. These files act as local environment-based prompt rules that standard coding extensions (such as Cline, Cursor, Roo-Code, Gemini, etc.) automatically parse and prioritize.

3. **Verification of the Target Structure**:
   - Ensure the `Prompts` directory is created in the project.
   - Generate this implementation plan inside the `Prompts` directory with the timestamp prefix `20260518.143952_implementation_plan.md`.

---

## Detailed Actions

### Step 1: Update Global Knowledge Item
We will modify the global rules in `C:\Users\ShibuPC\.gemini\antigravity\knowledge\ki_md_rule\artifacts\md_file_rules.md` to be extremely clear and detailed.

### Step 2: Create Project-Level Rule Files (`.clinerules` & `.cursorrules`)
We will create these files in the current active workspace root:
- [`.clinerules`](file:///x:/Projects_X/0_Active/8_Android_APK/Show_Two_Points_On_Map_APK_GITHUB/.clinerules)
- [`.cursorrules`](file:///x:/Projects_X/0_Active/8_Android_APK/Show_Two_Points_On_Map_APK_GITHUB/.cursorrules)

These rules will enforce the following logic for the AI:
> **MANDATORY RULES FOR MD FILES GENERATION:**
> 1. Any Markdown (`.md`) file created or edited by the AI (such as Implementation Plans, design notes, or analysis files) **MUST** be stored inside the `Prompts` directory at the root of the active workspace.
> 2. The filename **MUST** be prefixed with `yyyyMMdd.hhmmss_` (e.g., `20260518.143952_filename.md`), where the timestamp is retrieved from the current time metadata.
> 3. The exact raw user prompt that triggered the generation **MUST** be captured and displayed at the very top of the Markdown file under a `# User Prompt` header block.

### Step 3: Verify the implementation
Ensure that this Markdown file itself resides correctly in `x:\Projects_X\0_Active\8_Android_APK\Show_Two_Points_On_Map_APK_GITHUB\Prompts\20260518.143952_implementation_plan.md` and contains the user prompt at the top.
